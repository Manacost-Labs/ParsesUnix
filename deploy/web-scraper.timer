[Unit]
Description=Nightly ParserUnix crawl window

[Timer]
# Runs inside the 02:00-06:00 low-traffic window; randomized to avoid a stampede.
OnCalendar=*-*-* 02:00:00
RandomizedDelaySec=1800
Persistent=true

[Install]
WantedBy=timers.target
